import React from "react";
import Weather from "./Weather"; // ✅ Ensure Weather component is imported

function App() {
  return (
    <div>
      <Weather />  {/* ✅ Ensure Weather component is used */}
    </div>
  );
}

export default App; // ✅ Ensure App is exported as default
